# PL-300 間違えた問題ログ

このファイルはクイズセッションではるさんが間違えた問題を自動記録しています。

---

## 🚨 はるさんの最重要弱点（自己申告で「苦手」と明示）

### データフロー vs 共有データセット（4回目ミス／CS②-Q4・新問19・ランダム㉕）
**鉄則**: Power BIの再利用パターンは3層で整理する。

| レイヤー | 機能 | 何を再利用？ |
|---------|------|------------|
| **変換層（ETL）** | **データフロー** | Power Queryの変換ロジック |
| **モデル層** | 共有データセット | テーブル＋リレーション＋メジャー |
| **レポート層** | アプリ／.pbit | 完成したレポート画面 |

**問題文に出たら反射**:
- 「変換ロジック」「Power Query」「クレンジング」「ETL」「元データソースへのアクセス制限」 → **データフロー**
- 「完成済みモデル」「メジャー共有」「同じテーブル定義を全員が使う」 → 共有データセット

**よく引っかかる罠**: 「再利用」という単語だけで共有データセットを選んでしまう。**何を再利用するのか**を必ず確認する。

### ドリルダウン vs ドリルスルー（ランダム㉓）
- 「同じグラフで階層を掘る・年→月→日」 → **ドリルダウン**
- 「別ページに飛ぶ・フィルター引き継ぎ」 → **ドリルスルー**

### ALL vs ALLSELECTED（4回目ミス／Q87・CS④-Q1・新問31・ランダム㉔）
- 「**全国・全体・総〇〇**に対する割合」 → **ALL**
- 「**表示中・選択範囲**の合計」 → **ALLSELECTED**

---

## ランダム総復習セッション（2026-04-29 ㉑-㉚）

### ランダム㉓（2026-04-29）ドリルスルー
**はるさんの回答**: B ✗ ／ **正解**: C
**鉄則**: 「別ページに飛ぶ＋フィルター引き継ぎ」→ **ドリルスルー**。ドリルダウンは同一ビジュアル内の階層掘り下げ。

### ランダム㉔（2026-04-29）ALL vs ALLSELECTED
**はるさんの回答**: B ✗ ／ **正解**: A
**鉄則**: 「全国合計に対する割合」→ **ALL**（全フィルター解除）。ALLSELECTEDはスライサー範囲を尊重。**4回目ミス・最重要**。

### ランダム㉕（2026-04-29）データフロー vs 共有データセット
**はるさんの回答**: A ✗ ／ **正解**: B
**鉄則**: 「変換ロジックの一元管理・Power Query再利用」→ **データフロー**。共有データセットは完成済みモデルの共有。**4回目ミス・最重要・本人「苦手」申告**。

### ランダム㉗（2026-04-29）ALL(列名) vs REMOVEFILTERS()
**はるさんの回答**: D ✗ ／ **正解**: A
**鉄則**: 「1列だけフィルター解除」→ **ALL(列名)**。REMOVEFILTERS()引数なしはモデル全体のフィルター解除でやりすぎ。

### ランダム㉘（2026-04-29）RLS実装手順
**はるさんの回答**: D ✗ ／ **正解**: A
**鉄則**: ロール「定義」= **Desktop** ／ ユーザー「割り当て」= **Service**。**3回目ミス**（Q65・ランダム②に続く）。

---

## 新問セッション（2026-04-28〜）

### 新問18（2026-04-28）複数日付列のリレーション設計
**はるさんの回答**: D ✗  
**正解**: B

**問題概要**: Sales テーブルに OrderDate / ShipDate / DueDate の3列。それぞれで独立して分析できるモデル設計は？

**正解パターン**:
- Date テーブルを1つ作成
- 1本をアクティブリレーション、残り2本を**非アクティブ**リレーション
- DAX で `USERELATIONSHIP` を使って切り替える

**覚え方**: 「1つのDateテーブル」と書かれたら → **非アクティブ + USERELATIONSHIP** 一択

**⚠️ 注意**: 同テーマで過去にも2回間違えています（Q2・Q38）。超頻出パターン。

### ランダム⑭（2026-04-29）同期スライサーの表示/同期切り替え
**はるさんの回答**: A ✗
**正解**: B

**鉄則**: 「表示しないがフィルターは効かせたい」→ **同期スライサー（同期ON・表示OFF）**。「同期」と「表示」���独立したトグル。

---

### ランダム⑯（2026-04-29）SUMX vs SUM
**はるさんの回答**: A ✗
**正解**: B

**鉄則**: SUMXはSUMより**遅い**（行ごとに処理するため）。「行ごとの掛け算・割り算が必要」→ SUMX。「列をそのまま合計」→ SUM。

---

### ランダム②（2026-04-29）RLSのテスト方法
**はるさんの回答**: C ✗
**正解**: B

**鉄則**: 「RLSのテスト・動作確認」→ **Power BI Desktop の「ロールとして表示」**。サービスのセキュリティ設定はユーザー割り当て操作であってテスト機能ではない。

---

### ランダム④（2026-04-29）ALL vs フィルターコンテキスト
**はるさんの回答**: D ✗
**正解**: B

**鉄則**: `ALL(Sales)` → Salesテーブルの全フィルターを解除して全体合計が分母になる。分子は通常のフィルターコンテキスト（スライサーが効く）のまま。「全体の中での割合」を出す定番パターン。

---

### 新問61（2026-04-29）JSON列の展開
**はるさんの回答**: A ✗
**正解**: B

**鉄則**: 「JSON・Record型の列を複数列に分割」→ **列の展開（Expand）**。列の分割はテキストを区切り文字で切る操作でJSON構造を理解しない。

---

### 新問63（2026-04-29）ページ分割レポート
**はるさんの回答**: A ✗
**正解**: B

**鉄則**: ページ分割レポート = 「印刷・PDF出力・全行表示」に特化。請求書・明細書・帳票など。通常レポートはインタラクティブ・ビジュアル探索向け。

---

### 新問66（2026-04-29）計算グループ
**はるさんの回答**: A ✗
**正解**: B

**鉄則**: 「複数メジャーに同じ計算パターンを一括適用・一元管理」→ **計算グループ**。クイックメジャーは1個ずつ個別に作るもの。

---

### 新問69（2026-04-29）接続先の切り替え
**はるさんの回答**: D ✗
**正解**: B

**鉄則**: 「サービス上で接続先を切り替えたい」→ **Power Queryパラメーター**。展開パイプラインはコンテンツの昇格用。Q59と同じパターン。

---

### 新問53（2026-04-28）条件付き書式 vs データの色
**はるさんの回答**: B ✗
**正解**: C

**鉄則**: 「値によって色を変えたい」→ **条件付き書式**。「データの色」は固定色の設定のみ。

---

### 新問54（2026-04-28）展開パイプライン
**はるさんの回答**: A ✗
**正解**: C

**鉄則**: 「開発・テスト・本番の3環境管理・昇格」→ **展開パイプライン**一択。新問46でも同じ間違い（2回目）。

---

### 新問55（2026-04-28）四半期累計（QTD）
**はるさんの回答**: D ✗
**正解**: A

**鉄則**: 年累計=TOTALYTD／四半期累計=TOTALQTD／月累計=TOTALMTD。「〇〇累計」→ `TOTAL〇〇`を反射的に選ぶ。

---

### 新問56（2026-04-28）同一テーブルスライサーの自動連動
**はるさんの回答**: B ✗
**正解**: C

**鉄則**: 同じテーブルの列から作ったスライサーは追加設定なしに自動連動する。同期スライサーはページ間連動用。

---

### 新問58（2026-04-28）DirectQuery vs ライブ接続
**はるさんの回答**: B ✗
**正解**: C

**鉄則**: 「生のデータソースに直接クエリ」→ **DirectQuery**。ライブ接続はPower BIデータセット or SSASへの接続。

---

### 新問60（2026-04-28）ライセンスなしの社外共有
**はるさんの回答**: D ✗
**正解**: C

**鉄則**: 「ライセンスなし・社外・誰でも閲覧」→ **Webに公開（Publish to Web）**。ただし完全公開になるため機密データ不可。

---

### 新問43（2026-04-28）TOPN＋ALLの組み合わせ
**はるさんの回答**: D ✗
**正解**: C

**鉄則**: `ALL` ＋ `TOPN` → 全体から上位N件を抽出して集計。メジャーは必ず数値を返す。フィルターを変えるものではない（D は罠選択肢）。

---

### 新問45（2026-04-28）粒度違いのリレーション（日次vs月次）
**はるさんの回答**: B ✗
**正解**: C

**鉄則**: 「粒度が違うテーブルをリレーションしたい」→ **計算列で粒度を揃えて1対多**。多対多はほぼ罠。CS③-Q5と全く同じパターン（3回目）。

---

### 新問46（2026-04-28）定期メール送信
**はるさんの回答**: D ✗
**正解**: C

**鉄則**:
- 「定期的にレポートをメール送信」→ **サブスクリプション**
- 「閾値を超えたら通知」→ **データアラート**
- 「開発・テスト・本番の管理」→ **展開パイプライン**

---

### 新問31（2026-04-28）メジャーの小計・総計
**はるさんの回答**: D ✗
**正解**: A

**鉄則**: メジャーはフィルターコンテキストに応じて各集計レベルで自動再計算される。ALLは「全フィルターを無視して全体を強制参照」するときだけ使う。小計・総計の自動計算には不要。

---

### 新問33（2026-04-28）地図ビジュアルの階層設定
**はるさんの回答**: A ✗
**正解**: B

**鉄則**: 「国→地域→都市のドリルダウン階層」→ モデルビューで階層を作成してからビジュアルに追加。複数列をドラッグするだけでは正式な階層にならない。

---

### 新問35（2026-04-28）上位10%フラグ列
**はるさんの回答**: D ✗（ヒント後）
**正解**: B

**鉄則**: 「行ごとにフラグ・値を付けたい」→ 計算列。メジャーは行を持てない。フィルターは表示制御のみ。

---

### 新問37（2026-04-28）2テーブルの統合
**はるさんの回答**: A ✗
**正解**: B

**鉄則**:
- 「同じ構造のテーブルを縦につなげる」→ **追加（Append）**（SQLのUNION）
- 「キーで結合して列を横に足す」→ **マージ（Merge）**（SQLのJOIN）

---

### 新問39（2026-04-28）地図ビジュアルの誤表示
**はるさんの回答**: C ✗
**正解**: B

**鉄則**: 「地図ビジュアルで場所がずれる」→ まず**データカテゴリ**を確認。国・都市・郵便番号など地理列には必ずデータカテゴリを設定する。

---

### 新問23・25（2026-04-28）タイムインテリジェンス・日付テーブルのマーク
**はるさんの回答**: A ✗（新問23）／ わからん（新問25）
**正解**: B（両問とも）

**問題概要**: タイムインテリジェンス関数が動作しない原因は？/ 正しく動作させるために必要な設定は？

**鉄則**: **「日付テーブルとしてマーク」未設定 → タイムインテリジェンス関数が動かない**
- AUTO DATE/TIME OFF ＋ カスタムDateテーブル ＋ 「日付テーブルとしてマーク」がベストプラクティス
- DateKey整数列はリレーション用。タイムインテリジェンスとは無関係。

---

### 新問27（2026-04-28）インポートデータ量の削減
**はるさんの回答**: A ✗
**正解**: C

**鉄則**: **「インポート量を減らす」→ Power Query（上流）でフィルター**
- レポートフィルター・DAX FILTERは全データをロード後の「表示制御」にすぎない
- Q53でも同じパターンで出題済み

---

### 新問29（2026-04-28）前年同期比（YoY）DAX
**はるさんの回答**: C △（技術的には正しいが試験では不正解扱い）
**正解**: A

**鉄則**: **「前年同期比（YoY）」→ `SAMEPERIODLASTYEAR` 一択**
- `DATEADD(-1, YEAR)` も動作するが、試験では目的特化関数が優先される
- `PREVIOUSYEAR` / `PARALLELPERIOD` は「前年まるごと」→ 同期間比較にならない

---

### 新問19（2026-04-28）データフロー vs 共有データセット
**はるさんの回答**: A ✗  
**正解**: B

**問題概要**: 変換ロジックの一元化 ＋ 元データソースへのアクセス制限 → どの機能を使う？

**正解パターン**: **Power BI データフロー**
- 変換ロジック（Power Query処理）を一元管理して複数データセットから再利用
- アナリストにはDataflow経由のデータのみ公開 → 元ソースへのアクセスを制限できる

**覚え方**:
- 「変換ロジックの再利用」「元データソースへのアクセス制限」→ **データフロー**
- 共有データセット = モデル・レポートの共有（変換ロジックではない）

**⚠️ 注意**: CS②-Q4でも同じパターンで間違えています（2回目）。

---

## 過去セッションから引き継いだ弱点一覧

### 🚨 3回以上間違えた問題

#### Q65 RLSロールへのユーザー追加
**正解**: powerbi.com からデータセットの HR ロールにユーザーを追加する  
**よくある間違い**: Desktop操作・共有・DAX修正を選んでしまう  
**鉄則**: ロール「定義」= Desktop ／ ユーザー「割り当て」= サービス（powerbi.com）

---

### ⚠️ 2回間違えた問題

#### Q2・Q38・新問18 複数日付列のリレーション
**正解**: 非アクティブリレーション + USERELATIONSHIP  
**間違いパターン**: 「Dateテーブル複数作成」や「計算列で統合」を選ぶ

#### CS③-Q5 粒度違いリレーション
**正解**: 計算列で粒度を揃えて1対多にする  
**間違いパターン**: 多対多リレーションを選ぶ（ほぼ罠選択肢）

#### CS③-Q4 オンプレミス接続の構成
**鉄則**:
- 「オンプレミス」→ ゲートウェイ必要（DirectQueryでもImportでも）
- 「最大遅延X分」→ DirectQuery → スケジュール更新不要

#### CS③-Q2 役割別日付テーブル（Role-playing dimension）
**正解**: 同じDateテーブルを複製して別名で使う  
**間違いパターン**: 1本だけのリレーションを選ぶ

#### CS②-Q3 Build権限
**正解**: メンバー + Build権限  
**鉄則**: Read=閲覧のみ ／ Build=新規レポート作成可

#### Q114 エクスポート禁止
**正解**: Desktop のレポート設定 ＋ Service のレポート設定の**両方**を変更  
**間違いパターン**: データ読み込み設定やデータセット権限を選ぶ

#### ALL vs ALLSELECTED（Q87・CS④-Q1）
**鉄則**:
- 「総〇〇」「全体」「常に全カテゴリ」→ ALL
- 「表示される」「表示中」「選択した合計が100%」→ ALLSELECTED
